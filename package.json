{
  "name": "property-rental-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Static property rental platform. Served by a Node.js server locally. Deployed via Cloudflare Pages. Backend is Supabase (cloud-hosted).",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node generate-config.js"
  },
  "engines": {
    "node": ">=18"
  }
}
